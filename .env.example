# Example configuration for mcp-pandoc-ts
# Copy this file to .env and fill in the correct value for your environment.

# The full URL (including port) where the pandoc-host-service is running
# and accessible from the mcp-pandoc-ts container.
# Examples:
# PANDOC_HOST_URL=http://host.docker.internal:5001  # Docker Desktop (Mac/Win)
# PANDOC_HOST_URL=http://172.17.0.1:5001           # Common Linux bridge IP (verify!)
PANDOC_HOST_URL=http://localhost:5001